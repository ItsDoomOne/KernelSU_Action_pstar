--- a/selinux/sepolicy.c	2026-09-05 22:35:16.287138049 -0300
+++ b/selinux/sepolicy.c	2026-09-05 22:36:23.622674998 -0300
@@ -13,6 +13,9 @@
 #define selinux_policy selinux_ss
 #endif
 #include <linux/vmalloc.h>
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 1, 0)
+#include <linux/flex_array.h>
+#endif
 
 #include "sepolicy.h"
 #include "klog.h" // IWYU pragma: keep
@@ -177,6 +180,36 @@
     if (ret < 0)
         return false;
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 1, 0)
+    for (i = 0; i < db->te_avtab.nslot; i++) {
+        prev = NULL;
+        for (n = flex_array_get_ptr(db->te_avtab.htable, i); n;
+             prev = n, n = n->next) {
+            if (n != node)
+                continue;
+
+            if (prev)
+                prev->next = n->next;
+            else
+                flex_array_put_ptr(db->te_avtab.htable, i, n->next,
+                                   GFP_KERNEL);
+
+            if (db->te_avtab.nel > 0)
+                db->te_avtab.nel--;
+
+            if ((n->key.specified & AVTAB_XPERMS) && n->datum.u.xperms) {
+                shrink_size += sizeof(u8) + sizeof(u8) + sizeof(u32) * ARRAY_SIZE(n->datum.u.xperms->perms.p);
+            }
+            n->next = NULL;
+            flex_array_put_ptr(removed.htable, 0, n, GFP_KERNEL);
+            removed.nel = 1;
+            avtab_destroy(&removed);
+            if (db->len >= shrink_size)
+                db->len -= shrink_size;
+            return true;
+        }
+    }
+#else
     for (i = 0; i < db->te_avtab.nslot; i++) {
         prev = NULL;
         for (n = db->te_avtab.htable[i]; n; prev = n, n = n->next) {
@@ -203,6 +236,7 @@
             return true;
         }
     }
+#endif
 
     avtab_destroy(&removed);
     return false;
@@ -564,6 +598,7 @@
 };
 #endif
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 9, 0)
 static bool add_filename_trans(struct policydb *db, const char *s,
                                const char *t, const char *c, const char *d,
                                const char *o)
@@ -653,6 +688,91 @@
 out:
     return false;
 }
+#else
+static bool add_filename_trans(struct policydb *db, const char *s,
+                               const char *t, const char *c, const char *d,
+                               const char *o)
+{
+    struct type_datum *src, *tgt, *def;
+    struct class_datum *cls;
+    struct filename_trans *new_key = NULL;
+    int rc;
+
+    src = symtab_search(&db->p_types, s);
+    if (src == NULL) {
+        pr_warn("source type %s does not exist\n", s);
+        return false;
+    }
+    tgt = symtab_search(&db->p_types, t);
+    if (tgt == NULL) {
+        pr_warn("target type %s does not exist\n", t);
+        return false;
+    }
+    cls = symtab_search(&db->p_classes, c);
+    if (cls == NULL) {
+        pr_warn("class %s does not exist\n", c);
+        return false;
+    }
+    def = symtab_search(&db->p_types, d);
+    if (def == NULL) {
+        pr_warn("default type %s does not exist\n", d);
+        return false;
+    }
+
+    struct filename_trans key;
+    key.stype = src->value;
+    key.ttype = tgt->value;
+    key.tclass = cls->value;
+    key.name = (char *)o;
+
+    struct filename_trans_datum *trans =
+        hashtab_search(db->filename_trans, &key);
+    if (trans) {
+        // Duplicate, overwrite existing data and return
+        trans->otype = def->value;
+        return true;
+    }
+
+    new_key = (struct filename_trans *)kzalloc(sizeof(*new_key), GFP_KERNEL);
+    if (!new_key) {
+        pr_err("add_filename_trans: alloc filename_trans failed\n");
+        return false;
+    }
+    *new_key = key;
+    new_key->name = kstrdup(key.name, GFP_KERNEL);
+    if (!new_key->name) {
+        pr_err("add_filename_trans: kstrdup name failed\n");
+        goto free_key;
+    }
+
+    trans = (struct filename_trans_datum *)kcalloc(1, sizeof(*trans),
+                                                   GFP_KERNEL);
+    if (!trans) {
+        pr_err("add_filename_trans: alloc filename_trans_datum failed\n");
+        goto free_name;
+    }
+    trans->otype = def->value;
+
+    rc = hashtab_insert(db->filename_trans, new_key, trans);
+    if (rc) {
+        pr_err("add_filename_trans: hashtab_insert failed: %d\n", rc);
+        goto free_trans;
+    }
+
+    // filename_trans_ttypes is consulted with the raw ttype by
+    // filename_compute_type() on <5.9 kernels
+    ebitmap_set_bit(&db->filename_trans_ttypes, tgt->value, 1);
+    return true;
+
+free_trans:
+    kfree(trans);
+free_name:
+    kfree((void *)new_key->name);
+free_key:
+    kfree(new_key);
+    return false;
+}
+#endif
 
 static bool add_genfscon(struct policydb *db, const char *fs_name,
                          const char *path, const char *context)
@@ -718,6 +838,79 @@
         return false;
     }
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 1, 0)
+    // flex_array is not extensible, allocate bigger ones and copy the old data
+    struct flex_array *new_type_attr_map_array =
+        flex_array_alloc(sizeof(struct ebitmap), value, GFP_KERNEL | __GFP_ZERO);
+    if (!new_type_attr_map_array) {
+        pr_err("add_type: alloc type_attr_map_array failed\n");
+        return false;
+    }
+
+    struct flex_array *new_type_val_to_struct =
+        flex_array_alloc(sizeof(struct type_datum *), value,
+                         GFP_KERNEL | __GFP_ZERO);
+    if (!new_type_val_to_struct) {
+        pr_err("add_type: alloc type_val_to_struct_array failed\n");
+        return false;
+    }
+
+    struct flex_array *new_val_to_name_types =
+        flex_array_alloc(sizeof(char *), value, GFP_KERNEL | __GFP_ZERO);
+    if (!new_val_to_name_types) {
+        pr_err("add_type: alloc val_to_name failed\n");
+        return false;
+    }
+
+    if (flex_array_prealloc(new_type_attr_map_array, 0, value - 1,
+                            GFP_KERNEL | __GFP_ZERO) ||
+        flex_array_prealloc(new_type_val_to_struct, 0, value - 1,
+                            GFP_KERNEL | __GFP_ZERO) ||
+        flex_array_prealloc(new_val_to_name_types, 0, value - 1,
+                            GFP_KERNEL | __GFP_ZERO)) {
+        pr_err("add_type: prealloc failed\n");
+        return false;
+    }
+
+    int j;
+    void *old_elem;
+    for (j = 0; j < value - 1; j++) {
+        old_elem = flex_array_get(db->type_attr_map_array, j);
+        if (old_elem)
+            flex_array_put(new_type_attr_map_array, j, old_elem, GFP_KERNEL);
+    }
+    for (j = 0; j < value - 1; j++) {
+        old_elem = flex_array_get_ptr(db->type_val_to_struct_array, j);
+        if (old_elem)
+            flex_array_put_ptr(new_type_val_to_struct, j, old_elem, GFP_KERNEL);
+    }
+    for (j = 0; j < value - 1; j++) {
+        old_elem = flex_array_get_ptr(db->sym_val_to_name[SYM_TYPES], j);
+        if (old_elem)
+            flex_array_put_ptr(new_val_to_name_types, j, old_elem, GFP_KERNEL);
+    }
+
+    struct flex_array *old_fa = db->type_attr_map_array;
+    db->type_attr_map_array = new_type_attr_map_array;
+    if (old_fa)
+        flex_array_free(old_fa);
+
+    ebitmap_init(flex_array_get(db->type_attr_map_array, value - 1));
+    ebitmap_set_bit(flex_array_get(db->type_attr_map_array, value - 1),
+                    value - 1, 1);
+
+    old_fa = db->type_val_to_struct_array;
+    db->type_val_to_struct_array = new_type_val_to_struct;
+    if (old_fa)
+        flex_array_free(old_fa);
+    flex_array_put_ptr(db->type_val_to_struct_array, value - 1, type, GFP_KERNEL);
+
+    old_fa = db->sym_val_to_name[SYM_TYPES];
+    db->sym_val_to_name[SYM_TYPES] = new_val_to_name_types;
+    if (old_fa)
+        flex_array_free(old_fa);
+    flex_array_put_ptr(db->sym_val_to_name[SYM_TYPES], value - 1, key, GFP_KERNEL);
+#else
     struct ebitmap *new_type_attr_map_array =
         ksu_kvrealloc(db->type_attr_map_array, value * sizeof(struct ebitmap),
                       (value - 1) * sizeof(struct ebitmap));
@@ -754,6 +947,7 @@
 
     db->sym_val_to_name[SYM_TYPES] = new_val_to_name_types;
     db->sym_val_to_name[SYM_TYPES][value - 1] = key;
+#endif
 
     int i;
     for (i = 0; i < db->p_roles.nprim; ++i) {
@@ -792,7 +986,13 @@
 static void add_typeattribute_raw(struct policydb *db, struct type_datum *type,
                                   struct type_datum *attr)
 {
+#if LINUX_VERSION_CODE < KERNEL_VERSION(5, 1, 0)
+    struct ebitmap *sattr = flex_array_get(db->type_attr_map_array, type->value - 1);
+    if (!sattr)
+        return;
+#else
     struct ebitmap *sattr = &db->type_attr_map_array[type->value - 1];
+#endif
     ebitmap_set_bit(sattr, attr->value - 1, 1);
 
     struct hashtab_node *node;
